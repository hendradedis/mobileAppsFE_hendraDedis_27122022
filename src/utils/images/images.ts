const path = '../../assets/';
const image = path + 'image/';
const images = {
  image: {
    BG_IMAGE_1: require(image + 'background2.jpeg'),
    ICONWOM: require(image + 'wowIcons.png'),
    ICONPROFILE: require(image + 'man.png'),
  },
};

export default images;
